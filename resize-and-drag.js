$( ".resizable" ).resizable({
    handles:"n,e,s,w,ne,nw,se,sw"
    // helper: "resizable-helper"
    // containment: "parent"
    // animate: true
    // aspectRatio: true,
    // grid: [ 30,30 ] 
});
$( ".draggable").draggable({


});