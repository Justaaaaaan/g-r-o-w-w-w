$( ".resizable" ).resizable({
    handles:"n,e,s,w"
    // helper: "resizable-helper"
    // containment: "parent"
    // animate: true
    // aspectRatio: true,
    // grid: [ 30,30 ] 
});
$( ".draggable").draggable({


});